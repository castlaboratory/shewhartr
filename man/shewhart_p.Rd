% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart-p.R
\name{shewhart_p}
\alias{shewhart_p}
\title{p chart for the proportion of nonconforming items}
\usage{
shewhart_p(
  data,
  defects,
  n,
  index = NULL,
  limits = c("3sigma", "binomial"),
  rules = c("nelson_1_beyond_3s"),
  locale = getOption("shewhart.locale", "en"),
  verbose = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{defects}{Tidy-eval column reference for the count of
nonconforming items in each subgroup.}

\item{n}{Tidy-eval column reference for the subgroup size.}

\item{index}{Optional tidy-eval column reference for the x-axis.}

\item{limits}{One of \code{"3sigma"} (default; classical normal
approximation) or \code{"binomial"} (exact 0.00135 / 0.99865 binomial
quantiles, equivalent to the standard 3-sigma rate when n is
large).}

\item{rules}{Character vector of rule keys to apply. See
\code{\link[=shewhart_rules_available]{shewhart_rules_available()}}. Default applies Nelson 1 and 2.}

\item{locale}{One of \code{"en"}, \code{"pt"}, \code{"es"}, \code{"fr"}. Affects plot
labels and informative messages.}

\item{verbose}{Logical. Print progress messages? Defaults to the
\code{shewhart.verbose} option.}
}
\value{
A \link{shewhart_chart} object of subclass \code{shewhart_p}.
}
\description{
Constructs a p chart from counts of nonconforming items in subgroups
of size \code{n}. Subgroup sizes may vary; in that case, control limits
are computed per observation.
}
\details{
Standard 3-sigma limits use the normal approximation to the binomial:
\deqn{\bar p \pm 3 \sqrt{\bar p (1 - \bar p) / n_i}.}
For very small \code{n_i} or very small / very large \code{p_bar}, the
approximation deteriorates and exact binomial limits should be
preferred (\code{limits = "binomial"}).
}
\examples{
set.seed(1)
df <- data.frame(
  day     = 1:30,
  defects = rbinom(30, size = 100, prob = 0.05),
  n       = 100
)
fit <- shewhart_p(df, defects = defects, n = n, index = day)
print(fit)

}
\references{
Montgomery, D. C. (2019). \emph{Introduction to Statistical Quality
Control} (8th ed.). Wiley. Chapter 7.
}

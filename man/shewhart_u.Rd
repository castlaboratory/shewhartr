% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart-u.R
\name{shewhart_u}
\alias{shewhart_u}
\title{u chart for nonconformities per unit, variable inspection size}
\usage{
shewhart_u(
  data,
  defects,
  exposure,
  index = NULL,
  limits = c("3sigma", "poisson"),
  rules = c("nelson_1_beyond_3s"),
  locale = getOption("shewhart.locale", "en"),
  verbose = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{defects}{Tidy-eval column reference for raw defect counts.}

\item{exposure}{Tidy-eval column reference for the inspection size
(must be positive).}

\item{index}{Optional tidy-eval column reference for the x-axis.}

\item{limits}{One of \code{"3sigma"} (default; classical normal
approximation) or \code{"poisson"} (exact Poisson 0.00135 / 0.99865
quantiles, recommended when \code{c_bar < 10}).}

\item{rules}{Character vector of rule keys to apply. See
\code{\link[=shewhart_rules_available]{shewhart_rules_available()}}. Default applies Nelson 1 and 2.}

\item{locale}{One of \code{"en"}, \code{"pt"}, \code{"es"}, \code{"fr"}. Affects plot
labels and informative messages.}

\item{verbose}{Logical. Print progress messages? Defaults to the
\code{shewhart.verbose} option.}
}
\value{
A \link{shewhart_chart} object of subclass \code{shewhart_u}.
}
\description{
Constructs a u chart from defect counts and a per-observation
"exposure" (inspection size: square metres of fabric, hours of
operation, lines of code, etc.). For constant exposure use
\code{\link[=shewhart_c]{shewhart_c()}}.
}
\examples{
set.seed(1)
df <- data.frame(
  roll     = 1:25,
  defects  = rpois(25, lambda = 4 * runif(25, 0.5, 1.5)),
  m2       = runif(25, 0.5, 1.5)    # variable inspection size
)
fit <- shewhart_u(df, defects = defects, exposure = m2, index = roll)

}
\references{
Montgomery, D. C. (2019). \emph{Introduction to Statistical Quality
Control} (8th ed.). Wiley. Chapter 7.3.2.
}

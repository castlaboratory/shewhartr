% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models-growth.R
\name{fit_gompertz_dummy}
\alias{fit_gompertz_dummy}
\title{Convenience wrapper to fit \code{SSgompertzDummy} to a data frame}
\usage{
fit_gompertz_dummy(data, x, y, dummy, start = NULL, ...)
}
\arguments{
\item{data}{A data frame with the columns referenced by \code{x}, \code{y},
\code{dummy}.}

\item{x, y, dummy}{Tidy-eval column references.}

\item{start}{Optional named list of starting values (\code{Asym}, \code{b2},
\code{b3}, \code{Beta}). If \code{NULL}, the self-starting initialiser is used.}

\item{...}{Additional arguments passed to \code{\link[stats:nls]{stats::nls()}}.}
}
\value{
An object of class \code{nls}.
}
\description{
Convenience wrapper to fit \code{SSgompertzDummy} to a data frame
}
\examples{
\donttest{
set.seed(42)
df <- data.frame(
  x = seq(1, 10, length.out = 50),
  d = rep(c(0, 1), each = 25)
)
df$y <- 100 * exp(-2 * exp(-0.3 * df$x)) + 20 * df$d + rnorm(50, 0, 3)
fit <- fit_gompertz_dummy(df, x = x, y = y, dummy = d)
coef(fit)
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart-xbar-s.R
\name{shewhart_xbar_s}
\alias{shewhart_xbar_s}
\title{Xbar-S control chart for rational subgroups}
\usage{
shewhart_xbar_s(
  data,
  value,
  subgroup,
  sigma_method = c("sbar", "pooled_sd"),
  rules = c("nelson_1_beyond_3s", "nelson_2_nine_same"),
  locale = getOption("shewhart.locale", "en"),
  verbose = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{value}{Tidy-eval column reference for the measurement.}

\item{subgroup}{Tidy-eval column reference identifying the subgroup
(e.g. shift, batch, hour). All subgroups must have equal size.}

\item{sigma_method}{One of \code{"sbar"} (default; classical S-bar /
c4(n)) or \code{"pooled_sd"} (pooled within-subgroup SD; preferred
when subgroups have different sizes).}

\item{rules}{Character vector of rule keys to apply. See
\code{\link[=shewhart_rules_available]{shewhart_rules_available()}}. Default applies Nelson 1 and 2.}

\item{locale}{One of \code{"en"}, \code{"pt"}, \code{"es"}, \code{"fr"}. Affects plot
labels and informative messages.}

\item{verbose}{Logical. Print progress messages? Defaults to the
\code{shewhart.verbose} option.}
}
\value{
A \link{shewhart_chart} object of subclass \code{shewhart_xbar_s}.
}
\description{
Like \code{\link[=shewhart_xbar_r]{shewhart_xbar_r()}}, but uses the subgroup standard deviation
(S) instead of the range. Recommended for subgroup sizes greater
than 10, or when subgroup sizes differ.
}
\details{
Xbar-chart limits use \code{A3(n)}; S-chart limits use \code{B3(n)} and
\code{B4(n)}. When \code{sigma_method = "pooled_sd"}, sigma is estimated as
the pooled within-subgroup standard deviation.
}
\examples{
set.seed(1)
df <- data.frame(
  batch = rep(1:30, each = 12),
  y     = rnorm(360, mean = 80, sd = 0.6)
)
fit <- shewhart_xbar_s(df, value = y, subgroup = batch)
print(fit)

}
\references{
Montgomery, D. C. (2019). \emph{Introduction to Statistical Quality
Control} (8th ed.). Wiley. Chapter 6.4.
}

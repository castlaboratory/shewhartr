% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart-mcusum.R
\name{shewhart_mcusum}
\alias{shewhart_mcusum}
\title{Multivariate CUSUM control chart (Crosier 1988)}
\usage{
shewhart_mcusum(
  data,
  vars,
  index = NULL,
  target = NULL,
  cov = NULL,
  k = 0.5,
  h = NULL,
  locale = getOption("shewhart.locale", "en"),
  verbose = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{vars}{Tidy-select expression for the columns to monitor
jointly. At least 2 columns.}

\item{index}{Optional tidy-eval column for the x-axis.}

\item{target}{Optional length-\code{p} numeric vector. The in-control
mean. Defaults to \code{colMeans(data[, vars])}.}

\item{cov}{Optional \verb{p x p} covariance matrix. Defaults to
\code{cov(data[, vars])}.}

\item{k}{Reference value, in sigma units. Default \code{0.5}, tuned
for shifts of \verb{1 sigma}. Lower \code{k} makes the chart sensitive to
smaller shifts but increases false alarms.}

\item{h}{Decision interval. If \code{NULL}, looked up in the Crosier
(1988) Table 1 for \code{k = 0.5}, \code{ARL_0 ~ 200}, \verb{p = 2..10}.}

\item{locale}{One of \code{"en"}, \code{"pt"}, \code{"es"}, \code{"fr"}.}

\item{verbose}{Logical. Print progress messages?}
}
\value{
A \link{shewhart_chart} object of subclass \code{shewhart_mcusum}.
The augmented tibble has columns \code{.y} (the chart statistic),
\code{.upper} (the decision interval \code{h}), and \code{.flag_signal}.
}
\description{
Constructs a multivariate CUSUM chart for jointly monitoring \code{p}
correlated variables. Like the univariate CUSUM it accumulates
deviations from a target with a reference value \code{k} that decides
when the accumulator resets; unlike a Hotelling \code{T^2} chart it
carries memory across observations and so detects small persistent
shifts faster.
}
\examples{
set.seed(1)
Sigma <- matrix(c(1, 0.6, 0.6, 1), 2, 2)
base  <- MASS::mvrnorm(60, c(0, 0), Sigma)
shift <- MASS::mvrnorm(40, c(0.6, 0.6), Sigma)
df    <- data.frame(t = 1:100,
                    x1 = c(base[, 1], shift[, 1]),
                    x2 = c(base[, 2], shift[, 2]))
fit <- shewhart_mcusum(df, vars = c(x1, x2), index = t,
                       target = c(0, 0), cov = Sigma)
print(fit)
\donttest{
ggplot2::autoplot(fit)
}
}
\references{
Crosier, R. B. (1988). Multivariate Generalizations of Cumulative
Sum Quality-Control Schemes. \emph{Technometrics}, 30(3), 291-303.
\doi{10.1080/00401706.1988.10488402}

Pignatiello, J. J., & Runger, G. C. (1990). Comparisons of
Multivariate CUSUM Charts. \emph{Journal of Quality Technology}, 22(3),
173-186. \doi{10.1080/00224065.1990.11979237}
}

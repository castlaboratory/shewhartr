% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models-growth.R
\name{SSgompertzDummy}
\alias{SSgompertzDummy}
\title{Self-starting Gompertz with an additive dummy term}
\usage{
SSgompertzDummy(x, dummy, Asym, b2, b3, Beta)
}
\arguments{
\item{x}{Numeric vector. Independent variable.}

\item{dummy}{Numeric (typically 0/1) vector of the same length as \code{x}.}

\item{Asym}{Upper asymptote.}

\item{b2}{Curvature parameter.}

\item{b3}{Rate parameter.}

\item{Beta}{Dummy effect size.}
}
\value{
A numeric vector of fitted values, with attributes for
self-starting and an analytic gradient.
}
\description{
Extends the classical Gompertz form by adding a linear contribution
from a dummy covariate:
\deqn{y = \mathrm{Asym}\,\exp(-b_2\,\exp(-b_3 x)) + \beta\,d.}
Designed for use inside \code{\link[stats:nls]{stats::nls()}}: starting values for the four
parameters are computed automatically from the data, and the
analytic gradient is supplied for faster, more reliable
convergence.
}
\examples{
\donttest{
set.seed(42)
n <- 50
x <- seq(1, 10, length.out = n)
d <- rep(c(0, 1), each = n / 2)
y <- 100 * exp(-2 * exp(-0.3 * x)) + 20 * d + rnorm(n, 0, 3)
df <- data.frame(x = x, y = y, dummy = d)

fit <- nls(y ~ SSgompertzDummy(x, dummy, Asym, b2, b3, Beta), data = df)
summary(fit)
}

}
\references{
Same as \code{\link[=Gompertz]{Gompertz()}}.
}
\seealso{
\code{\link[stats:SSgompertz]{stats::SSgompertz()}} for the standard self-starting
Gompertz without a dummy term, \code{\link[=fit_gompertz_dummy]{fit_gompertz_dummy()}} for a
convenience wrapper.
}

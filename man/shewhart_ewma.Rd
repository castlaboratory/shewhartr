% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart-ewma.R
\name{shewhart_ewma}
\alias{shewhart_ewma}
\title{Exponentially Weighted Moving Average (EWMA) control chart}
\usage{
shewhart_ewma(
  data,
  value,
  index = NULL,
  target = NULL,
  sigma = NULL,
  lambda = 0.2,
  L = 2.7,
  steady_state = FALSE,
  rules = "nelson_1_beyond_3s",
  locale = getOption("shewhart.locale", "en"),
  verbose = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{value}{Tidy-eval column reference for the measurement.}

\item{index}{Optional tidy-eval column reference for the x-axis.}

\item{target}{Numeric. Process target / centre line. Defaults to
\code{mean(value)}.}

\item{sigma}{Numeric. Process sigma. Defaults to \code{MR_bar / 1.128}.}

\item{lambda}{Numeric in \verb{(0, 1]}. Smoothing constant. Default
\code{0.2}. Smaller lambda = more memory, more sensitive to small
shifts.}

\item{L}{Numeric. Width of the limits in standard errors of the
EWMA. Default \code{2.7}, which combined with \code{lambda = 0.2} yields
\code{ARL_0 ~ 370} (Lucas & Saccucci 1990).}

\item{steady_state}{Logical. Use asymptotic (constant) limits
instead of time-varying ones?}

\item{rules}{Character vector of runs rules to flag. Defaults to
Nelson 1 only — the EWMA's own limits already encode most of the
diagnostic power and the higher-order Nelson rules are not
designed for autocorrelated statistics.}

\item{locale}{One of \code{"en"}, \code{"pt"}, \code{"es"}, \code{"fr"}.}

\item{verbose}{Logical. Print progress messages?}
}
\value{
A \link{shewhart_chart} object of subclass \code{shewhart_ewma}. The
\code{augmented} slot has columns \code{.value} (the original observation),
\code{.ewma} (the smoothed statistic z_i, plotted on the chart), and
the usual \code{.center}, \code{.upper}, \code{.lower}, \verb{.flag_*}.
}
\description{
Constructs an EWMA chart for a single column of individual
measurements. The chart is more sensitive than a Shewhart I chart
to small but persistent shifts in the process mean, at the cost of
a longer reaction time to large shifts.
}
\details{
By default, sigma is estimated from the moving range of \code{value}
(Wheeler 1992 convention, \code{MR_bar / 1.128}); the centre is the mean
of \code{value}. Either can be overridden via \code{target} and \code{sigma} for
Phase II monitoring against pre-calibrated values.

Limits are time-varying by default — they widen out from \code{target}
as the EWMA "warms up" — converging to the asymptotic limits as
\code{i -> infinity}. Set \code{steady_state = TRUE} to use the asymptotic
limits everywhere (commonly chosen when calibrating from a long
baseline).
}
\examples{
set.seed(1)
df <- data.frame(
  day = 1:80,
  y   = c(rnorm(40, mean = 100, sd = 2),
          rnorm(40, mean = 101, sd = 2))   # 0.5 sigma shift
)
fit <- shewhart_ewma(df, value = y, index = day)
print(fit)
\donttest{
ggplot2::autoplot(fit)
}
}
\references{
Roberts, S. W. (1959). Control Chart Tests Based on Geometric Moving
Averages. \emph{Technometrics}, 1(3), 239-250.
\doi{10.1080/00401706.1959.10489860}

Lucas, J. M., & Saccucci, M. S. (1990). Exponentially Weighted
Moving Average Control Schemes: Properties and Enhancements.
\emph{Technometrics}, 32(1), 1-12.
\doi{10.1080/00401706.1990.10484583}

Montgomery, D. C. (2019). \emph{Introduction to Statistical Quality
Control} (8th ed.). Wiley. Chapter 9.
}

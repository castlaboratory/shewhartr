% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{shewhart_box_cox}
\alias{shewhart_box_cox}
\title{Box-Cox profile log-likelihood}
\usage{
shewhart_box_cox(data, value = NULL, lambda_grid = seq(-2, 2, by = 0.05))
}
\arguments{
\item{data}{A data frame, or a numeric vector. If a data frame,
\code{value} must be supplied.}

\item{value}{Tidy-eval column reference for the response (only when
\code{data} is a data frame).}

\item{lambda_grid}{Numeric vector of lambda values to evaluate.
Default: a fine grid from -2 to 2.}
}
\value{
An object of class \code{shewhart_box_cox} with components
\code{profile} (tibble of lambda vs. log-likelihood), \code{lambda_hat}
(the maximiser), \code{ci} (95\% CI). The object has its own
\code{print()} method.
}
\description{
Computes the profile log-likelihood for the Box-Cox power parameter
lambda, applied to a single positive numeric series (regression to
a constant) or to the residuals of a linear model.
}
\details{
Reports the lambda that maximises the profile likelihood and a
95\% confidence interval based on the chi-square approximation
to twice the log-likelihood drop (Box & Cox 1964, eq. 9).
}
\examples{
set.seed(1)
bc <- shewhart_box_cox(rlnorm(200, meanlog = 0, sdlog = 0.5))
bc$lambda_hat   # should be near 0 (log-normal data)

}
\references{
Box, G. E. P., & Cox, D. R. (1964). An Analysis of Transformations.
\emph{Journal of the Royal Statistical Society, Series B}, 26(2),
211-252. \doi{10.1111/j.2517-6161.1964.tb00553.x}
}

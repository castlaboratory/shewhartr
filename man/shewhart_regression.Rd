% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart-regression.R
\name{shewhart_regression}
\alias{shewhart_regression}
\title{Regression-based control chart for processes with trend}
\usage{
shewhart_regression(
  data,
  value,
  index,
  model = c("auto", "linear", "log", "loglog", "gompertz", "logistic"),
  formula = NULL,
  dummy = NULL,
  start_base = 10L,
  phase_changes = NULL,
  phase_rule = "nelson_2_nine_same",
  rules = c("nelson_1_beyond_3s", "nelson_2_nine_same"),
  sigma_method = c("mr", "median_mr", "biweight", "sd"),
  lower_bound = NA_real_,
  locale = getOption("shewhart.locale", "en"),
  verbose = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{value}{Tidy-eval column reference for the response.}

\item{index}{Tidy-eval column reference for the predictor (typically
time, but can be any continuous variable).}

\item{model}{Character. One of \code{"auto"} (Box-Cox guidance),
\code{"linear"}, \code{"log"} (fits \code{log(y + 1) ~ N}), \code{"loglog"},
\code{"gompertz"}, \code{"logistic"}. For full control, supply \code{formula}
instead.}

\item{formula}{Optional one-sided or two-sided formula referencing
columns in \code{data}. If provided, overrides \code{model}.}

\item{dummy}{Optional tidy-eval column reference for an additive
covariate (a "dummy" in the original v0.1 nomenclature; can be
any factor or numeric covariate the user wants to adjust for,
such as day-of-week effects or treatment indicators).}

\item{start_base}{Integer. Number of initial observations used to
estimate the first phase. Defaults to 10.}

\item{phase_changes}{Optional vector of index positions or values
at which to force a phase change. If \code{NULL}, phase changes are
detected automatically using the supplied \code{phase_rule}.}

\item{phase_rule}{Character. Runs rule used to detect new phases.
See \code{\link[=shewhart_rules_available]{shewhart_rules_available()}}. Default Nelson 2 (9 points
same side; ARL_0 ~ 256). For backward compatibility with v0.1.x,
use \code{"we_seven_same"} (7 points; ARL_0 ~ 64).}

\item{rules}{Character vector of rules to flag on the final chart.}

\item{sigma_method}{One of \code{"mr"} (default), \code{"median_mr"},
\code{"biweight"} (Tukey-style robust), or \code{"sd"}.}

\item{lower_bound}{Numeric scalar or \code{NA}. If non-\code{NA}, lower limit
is clipped at this value (commonly 0 for counts). Default \code{NA}
(no clipping).}

\item{locale}{Character. One of \code{"en"}, \code{"pt"}, \code{"es"}, \code{"fr"}.}

\item{verbose}{Logical. Print progress messages?}
}
\value{
A \link{shewhart_chart} object of subclass \code{shewhart_regression}.
The \code{fits} slot contains a list of fitted model objects (one per
phase).
}
\description{
Fits a chosen model to the data (linear, log, log-log, Gompertz,
logistic, or a user-supplied formula), then constructs control
limits around the fitted curve using the moving-range estimator on
the residuals (Wheeler 1992). Optionally detects phase changes
automatically via runs tests on the residuals and re-fits each
phase.
}
\details{
This is the package's flagship chart, intended for trended or
non-stationary processes for which classical Shewhart charts give
systematically wrong limits. See the vignette
\code{regression-charts} for a thorough discussion and examples.
}
\examples{
\donttest{
set.seed(1)
df <- data.frame(
  t = 1:60,
  y = c(1:30 * 0.5 + rnorm(30, sd = 0.5),    # phase 1: linear trend
        15 + 1:30 * 0.1 + rnorm(30, sd = 0.5)) # phase 2: shift + slowdown
)
fit <- shewhart_regression(df, value = y, index = t, model = "linear")
print(fit)
ggplot2::autoplot(fit)
}

}
\references{
Mandel, B. J. (1969). The Regression Control Chart. \emph{Journal of
Quality Technology}, 1(1), 1-9.
\doi{10.1080/00224065.1969.11980341}

Wheeler, D. J., & Chambers, D. S. (1992). \emph{Understanding Statistical
Process Control} (2nd ed.). SPC Press.

Box, G. E. P., & Cox, D. R. (1964). An Analysis of Transformations.
\emph{Journal of the Royal Statistical Society, Series B}, 26(2),
211-252. \doi{10.1111/j.2517-6161.1964.tb00553.x}
}

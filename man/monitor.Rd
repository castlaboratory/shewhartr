% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{monitor}
\alias{monitor}
\title{Phase II monitoring against pre-calibrated limits}
\usage{
monitor(data, chart)
}
\arguments{
\item{data}{A data frame with the same columns as the data used to
fit \code{chart}.}

\item{chart}{A pre-calibrated \link{shewhart_chart} object.}
}
\value{
A \link{shewhart_chart} object with \verb{$phase = "phase_2"} and
limits inherited from \code{chart}.
}
\description{
Applies the control limits (and rule set) from a calibrated
\link{shewhart_chart} object to fresh data. The new data must contain
the same columns used by the original chart constructor.
}
\details{
Limits are \emph{not} re-estimated; they are the limits stored on the
calibration object. Only the violation table is recomputed against
the new observations.
}
\examples{
set.seed(1)
base    <- data.frame(y = rnorm(50, mean = 100, sd = 2))
new_obs <- data.frame(y = rnorm(20, mean = 102, sd = 2))   # small shift
calib   <- calibrate(base, value = y, chart = "i_mr")
alarms  <- monitor(new_obs, calib)
alarms$violations

}
\references{
Woodall, W. H. (2000). Controversies and Contradictions in
Statistical Process Control. \emph{Journal of Quality Technology},
32(4), 341-350.
}

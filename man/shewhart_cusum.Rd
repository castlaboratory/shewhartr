% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart-cusum.R
\name{shewhart_cusum}
\alias{shewhart_cusum}
\title{Tabular CUSUM control chart}
\usage{
shewhart_cusum(
  data,
  value,
  index = NULL,
  target = NULL,
  sigma = NULL,
  k = 0.5,
  h = 4,
  locale = getOption("shewhart.locale", "en"),
  verbose = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{value}{Tidy-eval column reference for the measurement.}

\item{index}{Optional tidy-eval column reference for the x-axis.}

\item{target}{Numeric. Process target. Defaults to \code{mean(value)}.}

\item{sigma}{Numeric. Process sigma. Defaults to \code{MR_bar / 1.128}.}

\item{k}{Numeric. Reference value in units of sigma. Default \code{0.5},
tuned to detect 1-sigma shifts.}

\item{h}{Numeric. Decision interval in units of sigma. Default \code{4},
giving \code{ARL_0 ~ 168} for \code{k = 0.5}. Use \code{h = 5} for \code{ARL_0 ~ 465}
(Hawkins & Olwell 1998).}

\item{locale}{One of \code{"en"}, \code{"pt"}, \code{"es"}, \code{"fr"}.}

\item{verbose}{Logical. Print progress messages?}
}
\value{
A \link{shewhart_chart} object of subclass \code{shewhart_cusum}. The
\code{augmented} slot has columns \code{.value}, \code{.cusum_pos}, \code{.cusum_neg}
(the two accumulated statistics, both non-negative), \code{.upper}
(the decision interval \code{h * sigma}), and \code{.flag_signal}.
}
\description{
Constructs a two-sided tabular CUSUM chart for a single column of
individual measurements. Two cumulative statistics, \verb{C+} (upward)
and \verb{C-} (downward), are accumulated against a target with a
reference value \code{k}; an alarm fires when either crosses the
decision interval \code{h * sigma}.
}
\details{
By default, sigma is estimated from the moving range of \code{value}
(\code{MR_bar / 1.128}); the target is the mean of \code{value}. Either can
be overridden via \code{target} and \code{sigma} for Phase II monitoring
against pre-calibrated values.
}
\examples{
set.seed(1)
df <- data.frame(
  day = 1:80,
  y   = c(rnorm(40, mean = 100, sd = 2),
          rnorm(40, mean = 101, sd = 2))   # 0.5 sigma shift
)
fit <- shewhart_cusum(df, value = y, index = day)
print(fit)
\donttest{
ggplot2::autoplot(fit)
}
}
\references{
Page, E. S. (1954). Continuous Inspection Schemes. \emph{Biometrika},
41(1-2), 100-115. \doi{10.1093/biomet/41.1-2.100}

Hawkins, D. M., & Olwell, D. H. (1998). \emph{Cumulative Sum Charts and
Charting for Quality Improvement}. Springer.

Montgomery, D. C. (2019). \emph{Introduction to Statistical Quality
Control} (8th ed.). Wiley. Chapter 9.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_plotly.R
\name{as_plotly}
\alias{as_plotly}
\alias{as_plotly.default}
\alias{as_plotly.shewhart_chart}
\title{Convert a Shewhart chart to an interactive plotly figure}
\usage{
as_plotly(x, ...)

\method{as_plotly}{default}(x, ...)

\method{as_plotly}{shewhart_chart}(x, tooltip = c("x", "y"), ...)
}
\arguments{
\item{x}{A \link{shewhart_chart} object.}

\item{...}{Additional arguments forwarded to \code{\link[plotly:ggplotly]{plotly::ggplotly()}}.}

\item{tooltip}{Character vector of aesthetics to display in the
hover tooltip, as accepted by \code{\link[plotly:ggplotly]{plotly::ggplotly()}}. Defaults to
\code{c("x", "y")}.}
}
\value{
A \code{plotly} object (S3 class \code{plotly} / \code{htmlwidget}) ready
to print, embed in a Shiny app, or save with
\code{\link[htmlwidgets:saveWidget]{htmlwidgets::saveWidget()}}.
}
\description{
Produces an interactive HTML plotly version of the chart that
\code{\link[ggplot2:autoplot]{ggplot2::autoplot()}} would build for the same object. Useful for
dashboards, reports, and any context where hovering, zooming and
panning matters.
}
\details{
This is a separate function rather than an \code{autoplot()} argument
so that loading \code{shewhartr} does not pull \code{plotly} (and its full
transitive dependency tree) into every R session that uses the
package. \code{plotly} lives in \code{Suggests}; install it explicitly if
you want to use this function.
}
\examples{
\donttest{
if (requireNamespace("plotly", quietly = TRUE)) {
  set.seed(1)
  df  <- data.frame(t = 1:50, y = rnorm(50, mean = 100, sd = 2))
  fit <- shewhart_i_mr(df, value = y, index = t)
  as_plotly(fit)
}
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capability.R
\name{shewhart_capability}
\alias{shewhart_capability}
\title{Process capability indices Cp, Cpk, Pp, Ppk}
\usage{
shewhart_capability(
  data,
  lsl = NA_real_,
  usl = NA_real_,
  target = NA_real_,
  ci_level = 0.95,
  n_boot = 2000L,
  seed = NULL
)
}
\arguments{
\item{data}{A \link{shewhart_chart} object or a numeric vector.}

\item{lsl, usl}{Numeric scalars. Lower and upper specification
limits. At least one must be supplied.}

\item{target}{Numeric scalar. Optional process target. If missing,
defaults to the midpoint of \verb{(lsl, usl)}.}

\item{ci_level}{Numeric. Confidence level for bootstrap intervals.
Default \code{0.95}. Set to \code{NA} to skip bootstrap.}

\item{n_boot}{Integer. Number of bootstrap replicates. Default 2000.}

\item{seed}{Optional integer for reproducibility.}
}
\value{
A list of class \code{shewhart_capability} with point estimates
and (optionally) bootstrap CIs.
}
\description{
Computes the four classical capability indices for a Shewhart chart
or a raw vector. Optionally returns bootstrap confidence intervals.
}
\details{
For a \code{shewhart_chart} of type \code{i_mr}, \code{xbar_r}, or \code{xbar_s}, the
within-subgroup sigma stored on the chart object is used for
Cp/Cpk; the overall standard deviation of the raw data is used for
Pp/Ppk. For a numeric vector \code{data}, a single sigma is used for
both pairs (so Cp = Pp and Cpk = Ppk).

Capability indices are only meaningful when the process is in
statistical control (Phase I). The function emits a warning if the
supplied chart has any rule violations.
}
\examples{
\donttest{
set.seed(1)
df <- data.frame(y = rnorm(100, mean = 50, sd = 0.8))
fit <- shewhart_i_mr(df, value = y)
cap <- shewhart_capability(fit, lsl = 47, usl = 53, target = 50)
print(cap)
}

}
\references{
Kotz, S., & Lovelace, C. R. (1998). \emph{Process Capability Indices in
Theory and Practice}. Arnold.

Montgomery, D. C. (2019). \emph{Introduction to Statistical Quality
Control} (8th ed.). Wiley. Chapter 8.

Pearn, W. L., & Kotz, S. (2006). \emph{Encyclopedia and Handbook of
Process Capability Indices}. World Scientific.
}

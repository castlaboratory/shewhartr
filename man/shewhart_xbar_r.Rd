% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart-xbar-r.R
\name{shewhart_xbar_r}
\alias{shewhart_xbar_r}
\title{Xbar-R control chart for rational subgroups}
\usage{
shewhart_xbar_r(
  data,
  value,
  subgroup,
  rules = c("nelson_1_beyond_3s", "nelson_2_nine_same"),
  locale = getOption("shewhart.locale", "en"),
  verbose = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{value}{Tidy-eval column reference for the measurement.}

\item{subgroup}{Tidy-eval column reference identifying the subgroup
(e.g. shift, batch, hour). All subgroups must have equal size.}

\item{rules}{Character vector of rule keys to apply. See
\code{\link[=shewhart_rules_available]{shewhart_rules_available()}}. Default applies Nelson 1 and 2.}

\item{locale}{One of \code{"en"}, \code{"pt"}, \code{"es"}, \code{"fr"}. Affects plot
labels and informative messages.}

\item{verbose}{Logical. Print progress messages? Defaults to the
\code{shewhart.verbose} option.}
}
\value{
A \link{shewhart_chart} object of subclass \code{shewhart_xbar_r}.
}
\description{
Constructs a paired Xbar (subgroup mean) and R (subgroup range)
chart for measurements organised in rational subgroups of size 2 to
10. Sigma is estimated from the average within-subgroup range.
}
\details{
Xbar-chart limits use \code{A2(n)}; R-chart limits use \code{D3(n)} and
\code{D4(n)}. See \code{\link[=shewhart_constants]{shewhart_constants()}} for the tabulated values.
}
\examples{
set.seed(1)
df <- data.frame(
  batch = rep(1:25, each = 5),
  y     = rnorm(125, mean = 50, sd = 1.5)
)
fit <- shewhart_xbar_r(df, value = y, subgroup = batch)
print(fit)

}
\references{
Montgomery, D. C. (2019). \emph{Introduction to Statistical Quality
Control} (8th ed.). Wiley. Chapter 6.

Shewhart, W. A. (1931). \emph{Economic Control of Quality of Manufactured
Product}. D. Van Nostrand.
}

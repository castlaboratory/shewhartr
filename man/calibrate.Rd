% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{calibrate}
\alias{calibrate}
\title{Phase I calibration of a control chart}
\usage{
calibrate(data, ..., chart = "i_mr", trim_outliers = FALSE, max_trim_iter = 5L)
}
\arguments{
\item{data}{A data frame.}

\item{...}{Arguments passed to a chart constructor.}

\item{chart}{A character key naming the chart constructor:
\code{"i_mr"} (default), \code{"xbar_r"}, \code{"xbar_s"}, \code{"p"}, \code{"np"}, \code{"c"},
\code{"u"}, \code{"regression"}, \code{"ewma"}, \code{"cusum"}, \code{"hotelling"}.}

\item{trim_outliers}{Logical. If \code{TRUE}, iteratively drop
observations that violate the rules and re-estimate limits
(Montgomery 2019, Section 6.2.3).}

\item{max_trim_iter}{Integer. Maximum trimming iterations.}
}
\value{
A \link{shewhart_chart} object with \verb{$phase = "phase_1"}.
}
\description{
Convenience wrapper that fits a control chart and tags its phase as
\code{"phase_1"} (the default for any chart constructor). The intent is
to make Phase I usage \emph{explicit} in code: the practitioner
acknowledges that limits are being estimated.
}
\details{
Optionally drops violations from the in-control estimate ("trimmed"
calibration), per the iterative procedure described in Montgomery
(2019) Section 6.2.3: if any observation falls outside the limits,
it is removed and the limits are recomputed; iterate until either
all remaining points are in control or no further trimming is
possible.
}
\examples{
set.seed(1)
df <- data.frame(y = c(rnorm(40, mean = 100, sd = 2), 110, rnorm(20, 100, 2)))
calib <- calibrate(df, value = y, chart = "i_mr", trim_outliers = TRUE)
calib$n          # 60 if trim was avoided, fewer if outliers were removed

}
\references{
Woodall, W. H. (2000). Controversies and Contradictions in
Statistical Process Control. \emph{Journal of Quality Technology},
32(4), 341-350. \doi{10.1080/00224065.2000.11980013}

Montgomery, D. C. (2019). \emph{Introduction to Statistical Quality
Control} (8th ed.). Wiley. Section 6.2.3.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom.R
\name{augment.shewhart_chart}
\alias{augment.shewhart_chart}
\title{Augment new data with control-chart annotations}
\usage{
\method{augment}{shewhart_chart}(x, newdata = NULL, ...)
}
\arguments{
\item{x}{A \link{shewhart_chart} object.}

\item{newdata}{Optional data frame with the same columns as the
data used to fit \code{x}. If supplied, control limits are propagated
to the new rows (Phase II monitoring) and rule violations are
re-evaluated.}

\item{...}{Currently unused.}
}
\value{
A tibble. When \code{newdata = NULL}, the chart's augmented
tibble; otherwise the same shape but for \code{newdata}.
}
\description{
Returns the per-observation augmented tibble, optionally re-aligned
against fresh data passed via \code{newdata} (Phase II monitoring). When
\code{newdata} is \code{NULL}, returns the in-sample augmented tibble.
}
\examples{
set.seed(1)
df <- data.frame(y = rnorm(50))
fit <- shewhart_i_mr(df, value = y)
broom::augment(fit)

}

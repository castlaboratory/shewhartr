% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart-mewma.R
\name{shewhart_mewma}
\alias{shewhart_mewma}
\title{Multivariate EWMA control chart}
\usage{
shewhart_mewma(
  data,
  vars,
  index = NULL,
  target = NULL,
  cov = NULL,
  lambda = 0.1,
  h = NULL,
  steady_state = FALSE,
  locale = getOption("shewhart.locale", "en"),
  verbose = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{vars}{Tidy-select expression for the columns to monitor
jointly. Must select at least 2 columns.}

\item{index}{Optional tidy-eval column for the x-axis.}

\item{target}{Optional length-\code{p} numeric vector. The in-control
mean. Defaults to \code{colMeans(data[, vars])}.}

\item{cov}{Optional \verb{p x p} covariance matrix. Defaults to
\code{cov(data[, vars])}.}

\item{lambda}{Smoothing constant in \verb{(0, 1]}. Default \code{0.1}.}

\item{h}{Decision interval (UCL on the \code{T^2} statistic). If
\code{NULL}, looked up in the Prabhu & Runger (1997) table for
\code{ARL_0 ~ 200}.}

\item{steady_state}{Logical. Use the steady-state covariance
\code{(lambda / (2 - lambda)) * Sigma} everywhere instead of the
time-varying form? Default \code{FALSE}.}

\item{locale}{One of \code{"en"}, \code{"pt"}, \code{"es"}, \code{"fr"}.}

\item{verbose}{Logical. Print progress messages?}
}
\value{
A \link{shewhart_chart} object of subclass \code{shewhart_mewma}.
The augmented tibble has columns \code{.t2} (the MEWMA statistic),
\code{.upper} (the decision interval \code{h}), and \code{.flag_signal}.
}
\description{
Constructs a multivariate Exponentially Weighted Moving Average
(MEWMA) chart for jointly monitoring \code{p} correlated variables.
The chart is more sensitive than the Hotelling \code{T^2} chart to
small persistent shifts in the \emph{vector} mean, in the same way the
univariate EWMA is more sensitive than a Shewhart I chart.
}
\details{
By default \code{target} (the in-control mean vector) and \code{cov} (the
in-control covariance) are estimated from the data. For Phase II
monitoring, supply both explicitly so the limits use the
calibration values. The decision interval \code{h} is calibrated by
lookup in the Prabhu & Runger (1997) table for \code{ARL_0 ~ 200}; if
the \verb{(lambda, p)} combination is outside the tabulated range, the
user must pass \code{h} explicitly.
}
\examples{
set.seed(1)
Sigma <- matrix(c(1, 0.6, 0.6, 1), 2, 2)
base  <- MASS::mvrnorm(60, c(0, 0), Sigma)
shift <- MASS::mvrnorm(40, c(0.4, 0.4), Sigma)         # 0.4 sigma shift
df    <- data.frame(t = 1:100,
                    x1 = c(base[, 1], shift[, 1]),
                    x2 = c(base[, 2], shift[, 2]))
fit <- shewhart_mewma(df, vars = c(x1, x2), index = t,
                      target = c(0, 0), cov = Sigma,
                      lambda = 0.1)
print(fit)
\donttest{
ggplot2::autoplot(fit)
}
}
\references{
Lowry, C. A., Woodall, W. H., Champ, C. W., & Rigdon, S. E. (1992).
A Multivariate Exponentially Weighted Moving Average Control Chart.
\emph{Technometrics}, 34(1), 46-53.
\doi{10.1080/00401706.1992.10485232}

Prabhu, S. S., & Runger, G. C. (1997). Designing a Multivariate
EWMA Control Chart. \emph{Journal of Quality Technology}, 29(1), 8-15.
\doi{10.1080/00224065.1997.11979721}
}

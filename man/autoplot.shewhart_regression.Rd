% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot.R
\name{autoplot.shewhart_regression}
\alias{autoplot.shewhart_regression}
\title{Plot a regression control chart}
\usage{
autoplot.shewhart_regression(
  object,
  show_violations = TRUE,
  show_sigma_zones = FALSE,
  locale = NULL,
  phase_dates = NULL,
  legend_position = c("top", "inside"),
  ...
)
}
\arguments{
\item{object}{A \code{shewhart_regression} chart.}

\item{show_violations}{Logical. Ring out-of-control points?}

\item{show_sigma_zones}{Ignored; kept for a uniform signature.}

\item{locale}{Optional override for the chart's stored locale.}

\item{phase_dates}{Show where each phase ends in the legend, e.g.
"Phase 1 (until 2020-05-18)"? \code{NULL} (default) does so when the
index column is a \code{Date} or date-time; \code{TRUE} forces it for any
index (the last index value of each phase); \code{FALSE} turns it off.
The last phase ("Monitoring") never carries an end.}

\item{legend_position}{\code{"top"} (default) puts the legend above the
panel, wrapping to several rows when there are many phases;
\code{"inside"} stacks it in a single column in the top-left corner of
the panel, on the panel colour, like the figures of Ferraz et al.
(2020).}

\item{...}{Unused.}
}
\value{
A \code{ggplot} object.
}
\description{
The \code{autoplot()} method for \code{\link[=shewhart_regression]{shewhart_regression()}} charts: per
phase, a shaded band between dashed limits and a solid centre line;
observations coloured by phase; out-of-control points ringed.
}
\examples{
\donttest{
pe <- subset(cvd_brazil, region == "PE" & date <= as.Date("2020-06-20"))
fit <- shewhart_regression(pe, value = new_deaths, index = date,
                           model = "log", limits_scale = "model",
                           phase_rule = "we_seven_same")
ggplot2::autoplot(fit, legend_position = "inside")
ggplot2::autoplot(fit, phase_dates = FALSE, locale = "pt")
}

}

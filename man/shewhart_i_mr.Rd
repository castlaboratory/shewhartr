% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart-i-mr.R
\name{shewhart_i_mr}
\alias{shewhart_i_mr}
\title{Individuals and Moving Range (I-MR) control chart}
\usage{
shewhart_i_mr(
  data,
  value,
  index = NULL,
  sigma_method = c("mr", "median_mr", "biweight", "sd"),
  rules = c("nelson_1_beyond_3s", "nelson_2_nine_same"),
  locale = getOption("shewhart.locale", "en"),
  verbose = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{value}{Tidy-eval column reference for the measurement.}

\item{index}{Optional tidy-eval column reference for the x-axis
(date or integer). If \code{NULL} (default), the row index is used.}

\item{sigma_method}{One of \code{"mr"} (default; classical moving range),
\code{"median_mr"} (Tukey-style robust: median of moving ranges, with
bias correction), \code{"biweight"} (Tukey biweight midvariance),
\code{"sd"} (sample SD).}

\item{rules}{Character vector of rule keys to apply. See
\code{\link[=shewhart_rules_available]{shewhart_rules_available()}}. Default applies Nelson 1 and 2.}

\item{locale}{One of \code{"en"}, \code{"pt"}, \code{"es"}, \code{"fr"}. Affects plot
labels and informative messages.}

\item{verbose}{Logical. Print progress messages? Defaults to the
\code{shewhart.verbose} option.}
}
\value{
A \link{shewhart_chart} object of subclass \code{shewhart_i_mr}.
}
\description{
Constructs an I-MR chart for a single column of individual
measurements. Returns a \link{shewhart_chart} object that supports
\code{print()}, \code{summary()}, \code{autoplot()}, \code{tidy()}, \code{glance()} and
\code{augment()}.
}
\details{
Sigma is estimated from the moving range with \code{d2(2) = 1.128}; the
classical 3-sigma limits are equivalent to
\verb{x_bar +/- 2.660 * MR_bar}. The MR chart limits are
\verb{[0, D4(2) * MR_bar]} with \code{D4(2) = 3.267}.
}
\examples{
set.seed(1)
df <- data.frame(
  day = seq.Date(as.Date("2024-01-01"), by = "day", length.out = 60),
  y   = c(rnorm(40, mean = 100, sd = 2),
          rnorm(20, mean = 103, sd = 2))   # shift after position 40
)
fit <- shewhart_i_mr(df, value = y, index = day)
print(fit)
\donttest{
ggplot2::autoplot(fit)
}

}
\references{
Montgomery, D. C. (2019). \emph{Introduction to Statistical Quality
Control} (8th ed.). Wiley. Chapter 6.

Wheeler, D. J., & Chambers, D. S. (1992). \emph{Understanding Statistical
Process Control} (2nd ed.). SPC Press.
}

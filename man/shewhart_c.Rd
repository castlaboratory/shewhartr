% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart-c.R
\name{shewhart_c}
\alias{shewhart_c}
\title{c chart for the number of nonconformities}
\usage{
shewhart_c(
  data,
  defects,
  index = NULL,
  limits = c("3sigma", "poisson"),
  rules = c("nelson_1_beyond_3s"),
  locale = getOption("shewhart.locale", "en"),
  verbose = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{defects}{Tidy-eval column reference for the count of
nonconformities per inspection unit.}

\item{index}{Optional tidy-eval column reference for the x-axis.}

\item{limits}{One of \code{"3sigma"} (default; classical normal
approximation) or \code{"poisson"} (exact Poisson 0.00135 / 0.99865
quantiles, recommended when \code{c_bar < 10}).}

\item{rules}{Character vector of rule keys to apply. See
\code{\link[=shewhart_rules_available]{shewhart_rules_available()}}. Default applies Nelson 1 and 2.}

\item{locale}{One of \code{"en"}, \code{"pt"}, \code{"es"}, \code{"fr"}. Affects plot
labels and informative messages.}

\item{verbose}{Logical. Print progress messages? Defaults to the
\code{shewhart.verbose} option.}
}
\value{
A \link{shewhart_chart} object of subclass \code{shewhart_c}.
}
\description{
Constructs a c chart for counts of nonconformities (defects) per
inspection unit, where the unit (area, length, time, etc.) is
constant across observations. For variable inspection size use
\code{\link[=shewhart_u]{shewhart_u()}}.
}
\examples{
set.seed(1)
df <- data.frame(
  unit    = 1:40,
  defects = rpois(40, lambda = 6)
)
fit <- shewhart_c(df, defects = defects, index = unit)
fit_exact <- shewhart_c(df, defects = defects, index = unit,
                        limits = "poisson")

}
\references{
Montgomery, D. C. (2019). \emph{Introduction to Statistical Quality
Control} (8th ed.). Wiley. Chapter 7.3.

Ryan, T. P. (2011). \emph{Statistical Methods for Quality Improvement}
(3rd ed.). Wiley. Chapter 6 (on the inadequacy of 3-sigma limits
for low-mean Poisson counts).
}

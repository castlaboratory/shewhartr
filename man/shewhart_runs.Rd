% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runs-tests.R
\name{shewhart_runs}
\alias{shewhart_runs}
\title{Apply runs tests to a chart object or to raw vectors}
\usage{
shewhart_runs(
  x,
  rules = c("nelson_1_beyond_3s", "nelson_2_nine_same"),
  center = NULL,
  sigma = NULL
)
}
\arguments{
\item{x}{Either a \code{shewhart_chart} object (most common) or a numeric
vector of values. If a vector is supplied, \code{center} and \code{sigma}
must also be provided.}

\item{rules}{Character vector of rule keys to apply. Use
\code{\link[=shewhart_rules_available]{shewhart_rules_available()}} to see all options. Default applies
Nelson 1 (beyond 3 sigma) and Nelson 2 (9 same side), the most
commonly recommended pair.}

\item{center, sigma}{Numeric scalars or vectors. Required only when
\code{x} is a numeric vector. Ignored otherwise.}
}
\value{
A tibble with columns \code{position} (integer, the index where
the rule fired), \code{rule} (character key), \code{description} (character
label), \code{value} (the value at that position) and \code{severity}
(currently always \code{"out_of_control"}; reserved for future
warning-level rules).
}
\description{
Implements the eight rules of Nelson (1984, 1985) plus a Western
Electric "7 in a row" variant for backward compatibility. Returns a
tidy tibble of rule violations.
}
\examples{
set.seed(1)
x <- c(rnorm(20), 5, rnorm(20))    # one outlier at position 21
shewhart_runs(x, center = 0, sigma = 1)

}
\references{
Nelson, L. S. (1984). The Shewhart Control Chart -- Tests for
Special Causes. \emph{Journal of Quality Technology}, 16(4), 237-239.
\doi{10.1080/00224065.1984.11978921}

Nelson, L. S. (1985). Interpreting Shewhart Xbar Control Charts.
\emph{Journal of Quality Technology}, 17(2), 114-117.
\doi{10.1080/00224065.1985.11978941}

Western Electric Co. (1956). \emph{Statistical Quality Control Handbook}.
}

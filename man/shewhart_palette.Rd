% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette.R
\name{shewhart_palette}
\alias{shewhart_palette}
\title{Access the package's named colour palettes}
\usage{
shewhart_palette(
  name = c("phase_seq", "family", "signal", "neutral"),
  n = NULL
)
}
\arguments{
\item{name}{One of \code{"phase_seq"}, \code{"family"}, \code{"signal"},
\code{"neutral"}. See \emph{Details} for the contents and intended use of
each.}

\item{n}{Optional integer. Number of colours to return. If \code{NULL}
(default) the full palette is returned. If \code{n} exceeds the
palette length, a smooth ramp (\code{grDevices::colorRampPalette()})
is interpolated.}
}
\value{
A character vector of colour hex codes; for \code{family} and
\code{signal}, the vector is named.
}
\description{
Returns one of the curated colour vectors that every chart in the
package draws from. Useful when extending an \code{autoplot()} chart
with your own layers and you want them to match the rest of the
package's visual identity.
}
\details{
\describe{
\item{\code{phase_seq}}{Sequential palette for time-ordered phase
indices in regression / multi-phase charts. Cool blues at the
baseline, warming through neutral to terracotta as phase index
grows. Avoids saturated red (reserved for \code{signal}).}
\item{\code{family}}{Named categorical palette for the four chart
families: variables, attributes, memory-based, multivariate.
Identical to the colours used in the architecture diagram.}
\item{\code{signal}}{Two-colour palette: \code{in_control} (deep blue)
and \code{out_of_control} (firebrick). Used to colour violation
points on every chart so the alarm signal is consistent.}
\item{\code{neutral}}{Structural greys used by \code{shewhart_theme()} for
backgrounds, gridlines and text colour.}
}
}
\examples{
shewhart_palette("phase_seq", n = 4)
shewhart_palette("family")["multivariate"]
shewhart_palette("signal")
}
